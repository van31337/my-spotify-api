import { css } from "styled-components/macro";

const fonts = css`
  @font-face {
    font-family: "Circular Std";
    src: url("../fonts/circular-std--500") format(""),
    font-weight: 500;
    font-style: normal;
  }
`;

export default fonts;
